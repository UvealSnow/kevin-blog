---
import MainLayout from '@components/layouts/MainLayout.astro'
import Author from '@components/author.astro'
import { get } from '@src/cms'
import type { PostAuthor, PostTranslation } from '@src/types'

const { locale, slug } = Astro.params

interface Post {
	date_created: string
	translations: PostTranslation[]
	user_created: PostAuthor
}

interface Translation {
	translations: {
		slug: string
		languages_code: string
	}[]
}

export async function getStaticPaths() {
	const { data } = await get<Translation[]>('items/posts', {
		filter: {
			status: {
				_eq: 'published',
			},
		},
		fields: ['translations.slug', 'translations.languages_code'],
	})

	return data.flatMap((entry) => entry.translations.map((translation) => {
		const { languages_code: locale, slug } = translation
		return {
			params: { locale, slug },
		}
	}))
}

const { data } = await get<Post[]>('items/posts', {
	filter: {
		_and: [
			{
				translations: {
					slug: {
						_eq: slug,
					},
				},
			},
			{
				translations: {
					languages_code: {
						_eq: locale,
					},
				},
			},
		],
	},
	fields: [
		'translations.*',
		'date_created',
		'user_created.first_name',
		'user_created.last_name',
		'user_created.email',
		'user_created.location',
		'user_created.title',
		'user_created.description',
		'user_created.avatar',
	],
})

const { user_created: author, translations, date_created } = data[0]
const postTranslation = translations.find((t) => t.languages_code === locale)!
const createdAt = new Date(date_created).toLocaleString('en-GB', {
	day: '2-digit',
	month: '2-digit',
	year: 'numeric',
	hour: '2-digit',
	minute: '2-digit',
	hour12: true,
})
---

<MainLayout locale={locale} title="" description="">
	<div>
		<h1 class="text-5xl">{postTranslation.name}</h1>
		<p class="text-xs mt-2 mb-8">First published: {createdAt}</p>
		<Author author={author} />
		<div set:html={postTranslation.content} />
	</div>
</MainLayout>
